# Linux-Scripts
Bash Shell Scripts 

To install scripts, 
Download the file,
Open a terminal in the folder that contains the file,
Change the permissions with 
chmod +x ./(file.sh)
Use the script with 
./(file.sh)
